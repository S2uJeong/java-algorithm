
# 분수로 생각하기
   1. 공통 분모 만들기, 분자도 같이 곱해주기
   2. 분자 더하기
   3. 약분 -> 약분,, 최대공약수 이용?? -> 유클리드 호제법??? 이걸로,,해보자,,
    3-1. 최대공약수를 공통분모, 더한 분자에 나눠 준다. 근데 한번으로 안되는 경우도 있을 수 있지 않나?
      -> 분수를 분모, 분자의 최대 공약수를 구해 각각 나눠주게 되면 기약분수가 된다.
   4. 배열에 넣어주기

 ## 이슈발생
 - 정확도가 40% 밖에 되지 않았음
 - 테스트 케이스로 1,2,1,2 를 입력해본 결과 예상했던 1,1 이 아닌 4,1이 나오는거 확인하며 문제점 확인
 -> 만약 입력되는 두 분수의 값이 분모가 같을 시 다른 계산을 하는 if문을 추가함
 -> 최대공약수 구하는 부분에서 gcd를 할때 변수에 담아두고 사용하지 않아, 원하는 결과가 나오지 않음

# 소수로 생각하기
  1. 분수값을 소수로 변수에 저장  변수 = 분자/분모
     -> 형변환이 너무 까다로울 듯
  2. 포기

